apiVersion: v1
kind: Service
metadata:
  name: {{ include "kalki-waf.name" . }}-api
  labels:
    {{- include "kalki-waf.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kalki-waf.api.selectorLabels" . | nindent 4 }}
